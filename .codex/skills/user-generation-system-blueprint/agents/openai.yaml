version: 1
interface:
  display_name: "User Generation System Blueprint"
  short_description: "Convert product requirements into an implementation-grade multi-page system blueprint."
  default_prompt: "Use this skill when a project needs scoped routes, roles, core flows, cross-page consistency, responsive strategy, and stack contracts before template composition or coding begins. 适用于从需求到系统蓝图的结构化建模。"
