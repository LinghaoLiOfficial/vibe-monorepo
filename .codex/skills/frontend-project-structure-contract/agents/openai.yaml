version: 1
interface:
  display_name: "Frontend Structure Contract"
  short_description: "Define and enforce a canonical frontend project directory structure across template and code-generation runs."
  default_prompt: "Use this skill when multiple runs must keep consistent frontend structure boundaries (frontend/src/app, components, features, styles), route placement rules, and theme-entrypoint ownership."
