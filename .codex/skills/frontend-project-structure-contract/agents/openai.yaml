version: 1
interface:
  display_name: "Frontend Structure Contract"
  short_description: "Define and enforce the current repo frontend minimal skeleton and optional extension boundaries."
  default_prompt: "Use this skill when runs must align generated frontend code to the current minimal skeleton (frontend/src/app, frontend/src/lib, frontend/src/styles) while documenting optional extensions like components/features."
