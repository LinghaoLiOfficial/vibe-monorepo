version: 1
interface:
  display_name: "User Generation Nextjs React Code Generation"
  short_description: "Generate frontend code first, then emit frontend-derived API contract input for downstream backend stages."
  default_prompt: "Use this skill when frontend-first fullstack runs need responsive Next.js implementation plus a structured frontend-api-contract-input artifact for API contract design."
