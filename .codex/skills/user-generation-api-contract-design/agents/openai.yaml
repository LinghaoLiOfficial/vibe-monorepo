version: 1
interface:
  display_name: "User Generation API Contract Design"
  short_description: "Produce implementation-grade API contracts with explicit backend spec and structure alignment checks."
  default_prompt: "Use this skill when frontend-first runs need a strict API contract before backend code generation, including BACKEND_SPEC alignment and structure consistency evidence."
