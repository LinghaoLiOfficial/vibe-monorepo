version: 1
interface:
  display_name: "User Generation Visual QA Iterative Fix"
  short_description: "Run post-integration visual QA and minimal frontend fixes until responsive/presentation gates pass."
  default_prompt: "Use this skill after fullstack integration QA passes, when the run needs final visual and responsive quality closure with minimal scoped fixes."
